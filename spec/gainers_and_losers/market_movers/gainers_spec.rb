require 'spec_helper'

describe GainersAndLosers::MarketMovers::Gainers do
  before(:each) do
    @gainers = GainersAndLosers::MarketMovers::Gainers.new
  end
  
  it 'should fail' do
    raise 'fail'
  end
end
